import { ContactNumberDirective } from './contact-number.directive';

describe('ContactNumberDirective', () => {
  it('should create an instance', () => {
    const directive = new ContactNumberDirective();
    expect(directive).toBeTruthy();
  });
});
